<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Profesores Hola al Mundo</title>
  <script src="https://kit.fontawesome.com/3964fc737d.js" crossorigin="anonymous"></script>
  <link
    href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap"
    rel="stylesheet">
  <link href="/css/normalize.css" rel="stylesheet">
  <link href="/css/seteoPerfilProfes-1.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/partialsCss/partials.css">
  <script src="/js/validationPackages.js"></script>
</head>

<body>
  <!-- Header -->
  <%- include('../partials/header.ejs')%>
    <main class="container-main">
      <section class="container-filtros">
        <div class="formPaquetes">
          <div class="container-header">
            <h2>Crea tus paquetes</h2>
          </div>
          <form name="formPackage" action="/teachers/packages" method="POST" id="crearPackage">
            <div class="contentInput"><!-- nombre -->
                <label for="description"></label>
                <input placeholder="Nombre del paquete" type="text" name="description" class="input-form" id="descriptionPackage"
                    <%= locals.errors && errors.description ? 'is-invalid' : null %>
                    value="<%= locals.oldData ? oldData.description : null %>"
                    >
                    <% if(locals.errors && errors.description) { %>
                      <div class="text-danger" style="color: red">
                        <%= errors.description.msg %>
                      </div>
                    <% } %>
            </div>
            <div><!-- lenguaje -->
              <label for="language:"></label>
              <select name="language" id="language" class="input-form" <%=locals.errors && errors.language ? 'is-invalid' : null %>>
                <option value="" disabled selected>- Seleciona el idoma - </option>
                <% allLanguages.forEach(language => { %>
                  <option value="<%= `${language.dataValues.language}` %>">
                    <%= locals.oldData && (language.dataValues.language===oldData.language) ? "select" : null %>
                      <%= `${language.dataValues.language}` %>
                  </option>
                  <% }) %>
              </select>
              <% if (locals.errors && errors.language) { %>
                <div class="text-danger" style="color: red" style="font-weight: bold">
                  <%= errors.language.msg %>
                </div>
                <% } %>
            </div>
            <div><!-- Dias -->
              <select name="week_days" class="input-form" id="week_days"
              <%=locals.errors && errors.week_days ? 'is-invalid' : null %>>
                <option value="" disabled selected>- Seleciona el dias - </option>
                <% allWeekDays.forEach(days=> { %>
                  <option value="<%= `${days.dataValues.days}` %>">
                    <%= locals.oldData && (days.dataValues.days===oldData.days) ? "select" : null %>
                      <%= `${days.dataValues.days}` %>
                  </option>
                  <% }) %>
              </select>
              <% if (locals.errors && errors.week_days) { %>
                <div class="text-danger" style="color: red" style="font-weight: bold">
                  <%= errors.week_days.msg %>
                </div>
                <% } %>
            </div>
            <div><!-- HORAS -->
              <select name="week_times" id="week_times" class="input-form" 
              <%=locals.errors && errors.week_times ? 'is-invalid' : null %>>
                <option value="" disabled selected>- Selecionar Hora - </option>
                <% allWeekTimes.forEach(times=> { %>
                  <option value="<%= `${times.dataValues.times}` %>">
                    <%= locals.oldData && (times.dataValues.times===oldData.times) ? "select" : null %>
                      <%= `${times.dataValues.times}` %>
                  </option>
                  <% }) %>
              </select>
              <% if (locals.errors && errors.week_times) { %>
                <div class="text-danger" style="color: red" style="font-weight: bold">
                  <%= errors.week_times.msg %>
                </div>
                <% } %>
            </div>
            <div><!-- Levels -->
              <select name="level" id="level" class="input-form" 
              <%=locals.errors && errors.levels ? 'is-invalid' : null %>>
                <option value="" disabled selected>- Selecionar Nivel - </option>
                <% allLevels.forEach(levels => { %>
                  <option value="<%= `${levels.dataValues.levels}` %>">
                    <%= locals.oldData && (levels.dataValues.levels===oldData.levels) ? "select" : null %>
                      <%= `${levels.dataValues.levels}` %>
                  </option>
                  <% }) %>
              </select>
              <% if (locals.errors && errors.level) { %>
                <div class="text-danger" style="color: red" style="font-weight: bold">
                  <%= errors.level.msg %>
                </div>
                <% } %>
            </div>
            <div class="contentInput"><!-- TEMAS DE INTERES -->
              <label for="topics"></label>
              <input placeholder="Tematica de interes" id="topics" type="text" name="topics" class="input-form" 
                  <%= locals.errors && errors.topics ? 'is-invalid' : null %>
                  value="<%= locals.oldData ? oldData.topics : null %>"
                  >
                  <% if(locals.errors && errors.topics) { %>
                    <div class="text-danger" style="color: red">
                      <%= errors.topics.msg %>
                    </div>
                  <% } %>
            </div>
            <div><!-- MODALIDAD -->
              <select name="types" id="types" class="input-form" 
              <%=locals.errors && errors.types ? 'is-invalid' : null %>>
                <option value="" disabled selected>- Selecionar Nivel - </option>
                <% allTypes.forEach(types => { %>
                  <option value="<%= `${types.dataValues.types}` %>">
                    <%= locals.oldData && (types.dataValues.types===oldData.types) ? "select" : null %>
                      <%= `${types.dataValues.types}` %>
                  </option>
                  <% }) %>
              </select>
              <% if (locals.errors && errors.types) { %>
                <div class="text-danger" style="color: red" style="font-weight: bold">
                  <%= errors.types.msg %>
                </div>
                <% } %>
            </div>
            <div class="contentInput"><!-- precio -->
              <div class="inputPaquete">
                <input placeholder="Precio por hora:" type="text" name="price" id="price" class="price" <%=locals.errors
                  && errors.price ? 'is-invalid' : null %> value="<%= locals.oldData ? oldData.price : null %>">
                  <% if(locals.errors && errors.price) { %>
                    <div class="text-danger" style="color: red">
                      <%= errors.price.msg %>
                    </div>
                    <% } %>
              </div>
            </div>
            <div class="contentInput"><!-- capacidad max -->
              <label for="cap_max"></label>
              <input placeholder="cap max" id="cap_max" type="text" name="cap_max" class="input-form" 
                  <%= locals.errors && errors.cap_max ? 'is-invalid' : null %>
                  value="<%= locals.oldData ? oldData.cap_max : null %>"
                  >
                  <% if(locals.errors && errors.cap_max) { %>
                    <div class="text-danger" style="color: red">
                      <%= errors.cap_max.msg %>
                    </div>
                  <% } %>
            </div>
            

            <div class="btnNext">
              <button class="next" type="submit">crear<i class="fas fa-check"></i></i> </button>
            </div>
            <ul class=".errores">

            </ul>
        </div>

        </form>
        </div>
      </section>
      <h2 class="misPaq">Mis paquetes</h2>
      <div class="crearPaquete">
        <!-- card 1 -->
        <div>
          <div class="serviceImg">
            <span class="discountTag"> $5000</span>
            <i class="fas fa-comments"></i>
            <h4>
              Conversación
            </h4>
            <p class="description">
              Lorem, ipsum dolor sit amet consectetur adipisicing elit. Ipsa,
            </p>
          </div>
          <button class="serviceInfo">
            <i class="fas fa-pen"></i>
            Modificar
          </button>
        </div>
        <!-- card 1 -->
        <div>
          <div class="serviceImg">
            <span class="discountTag"> $5000</span>
            <i class="fas fa-comments"></i>
            <h4>
              Conversación
            </h4>
            <p class="description">
              Lorem, ipsum dolor sit amet consectetur adipisicing elit. Ipsa,
            </p>
          </div>
          <button class="serviceInfo">
            <i class="fas fa-pen"></i>
            Modificar
          </button>
        </div>
        <!-- card 1 -->
        <div>
          <div class="serviceImg">
            <span class="discountTag"> $5000</span>
            <i class="fas fa-comments"></i>
            <h4>
              Conversación
            </h4>
            <p class="description">
              Lorem, ipsum dolor sit amet consectetur adipisicing elit. Ipsa,
            </p>
          </div>
          <button class="serviceInfo">
            <i class="fas fa-pen"></i>
            Modificar
          </button>
        </div>
        <!-- card 1 -->
        <div>
          <div class="serviceImg">
            <span class="discountTag"> $5000</span>
            <i class="fas fa-comments"></i>
            <h4>
              Conversación
            </h4>
            <p class="description">
              Lorem, ipsum dolor sit amet consectetur adipisicing elit. Ipsa,
            </p>
          </div>
          <button class="serviceInfo">
            <i class="fas fa-pen"></i>
            Modificar
          </button>
        </div>

      </div>

    </main>
    <!-- Footer -->
    <%- include('../partials/footer.ejs')%>


      <script src="/js/scriptHeader.js"></script>
</body>

</html>